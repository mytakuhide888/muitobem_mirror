import logging

logger = logging.getLogger(__name__)


def dispatch_scheduled_posts():
    """APPROVEDな予約投稿を送信するスタブ"""
    logger.info("dispatch_scheduled_posts called")
    return 0


def dispatch_broadcasts():
    """一斉配信を送信するスタブ"""
    logger.info("dispatch_broadcasts called")
    return 0
